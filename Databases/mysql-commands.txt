#> mysqldump -u root -p [database_name] > restoreFile.sql -> Restaurar BD
mysql> USE [database_name] -> Cambiar de base de datos
mysql> SHOW TABLES; -> Mostrar tablas



#> mysql -h localhost -u root -p -> Iniciar sesion
mysql> show databases; -> Ver bases de datos
mysql> use <databaseName>; -> Ingresar a una base de datos
mysql [database]> show tables; -> Ver tablas de una base de datos
mysql [database]> describe <tableName>; -> Ver atributos de una tabla
mysql [database]> create database <tableName>; -> Crear base de datos
mysql [database]> drop database <tableName>; -> Borrar base de datos

#> mysqldump -h [host] -u [user] -p [database] > [nameFile].sql -> Crear copia base de datos
  |> mysqldump -h localhost -u root -p dbmuni_core > 202310171258-backup.sql
  |> mysqldump --ssl-mode=DISABLED -h 10.0.9.81 -u sgd -p db_tramite_2025 > 202503211717-dbsgdcore.sql

#> mysql -h [host] -u [user] -p < [nameFile].sql -> Restaurar base de datos


CREAR USUARIO UNA VEZ INSTALADO MYSQL
sudo mysql -u root   -> Ingresar con usuario root

-- 1. Crear el usuario
CREATE USER 'sgd'@'%' IDENTIFIED BY '@sgd2024';

-- 2. Otorgar todos los privilegios en todas las bases de datos
GRANT ALL PRIVILEGES ON *.* TO 'sgd'@'%' WITH GRANT OPTION;

-- 3. Aplicar los cambios
FLUSH PRIVILEGES;
